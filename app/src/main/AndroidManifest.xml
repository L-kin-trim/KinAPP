<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kin"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.admin.AdminCenterActivity" />
        <activity android:name=".ui.MessagesActivity" />
        <activity android:name=".ui.PostDetailActivity" />
        <activity android:name=".ui.PublishEditorActivity" />
        <activity android:name=".ui.DraftsActivity" />
        <activity android:name=".ui.MessageBoardActivity" />
        <activity android:name=".ui.MyReportsActivity" />
        <activity android:name=".ui.UserProfileActivity" />
        <activity android:name=".ui.AuthActivity" />
        <activity android:name=".ui.LaunchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>
